export const config = {
    FOV   : 90,    // Camera frustum vertical field of view.
    ASPECT: 4/3,   // Camera frustum aspect ratio.
    NEAR  : 0.1,   // Camera frustum near plane.
    FAR   : 1000   // Camera frustum far plane.

}